/**
 * This file is just for demoing Mocha and Chai and preserving the directory structure
 */

const { assert } = require('chai');

describe('Testing demo tests', () => {
  it('Should just pass', () => {
    assert.equal(true, true);
  });
});
